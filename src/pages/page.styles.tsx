import { Box, styled } from "@mui/material";

export const PageContainer = styled(Box)(() => ({
  width: "100%",
  height: "100%",
  overflowY: "auto",
  overflowX: "hidden",
  padding:"0px 40px"
}));
