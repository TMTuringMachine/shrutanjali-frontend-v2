import { styled, Box, Grid } from '@mui/material';

export const ContinueListeningSection = styled(Box)(() => ({
  width: '100%',
  marginTop: '10px',
  display: 'flex',
  flexWrap: 'wrap',
  //   backgroundColor: "red",
}));
